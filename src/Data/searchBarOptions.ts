//const options = ["All", "option2", "option3"];
interface Options {
  name: string;
}

const options: Options[] = [{ name: "All" }, { name: "Option2" }, { name: "Option3" }];

export default options;
